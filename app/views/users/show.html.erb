<div class="container">
  <div class="row">
    <div class="col-md-2">

    </div>
    <div class="col-md-8">
      <div class="user_info">
        <%= gravatar_for @user,250 %>
        <h1 ><%= @user.name %></h1>
        <%=link_to "ユーザ情報の編集",edit_user_registration_path%>
        <p>メールアドレス : <%= @user.email %></p>
        <p>プロフィール : <%= @user.profile %></p>
      </div>

      <!-- URLコピーボタン：何回でもコピーできるようにする -->
      <div class="copy-button">
      <button type="button" id="copy-page"><span id="cAction">URLをコピー</span></button>
        <script>
        document.getElementById("copy-page").onclick = function() {
        $(document.body).append("<textarea id=\"copyTarget\" style=\"position:absolute; left:-9999px; top:0px;\" readonly=\"readonly\">" +location.href+ "</textarea>");
        let obj = document.getElementById("copyTarget");
        let range = document.createRange();
        range.selectNode(obj);
        window.getSelection().addRange(range);
        document.execCommand('copy');
        document.getElementById("cAction").innerHTML = "コピーしました";
          };
        </script>
      </div>

      <div class="user_lists">

        <div class="music_list">
          <h3>MUSIC LIST</h3>
          <%= link_to "編集する","/users/#{current_user.id}/music_index"%>
            <!-- ユーザ詳細にはベスト５くらいまでを表示して一覧＆編集ページにリンクを飛ばす -->
          <%@user.my_musics.first(5).each do |my_music|%>
              <%artist = RSpotify::Artist.find(my_music.artist_id)%>
              <%=artist.name%>
              <%= link_to("リストから削除する","/musics/#{artist.id}/destroy", {method: "post"})%>

              <% if artist.images.any?%>
                <%image = artist.images[2]%>
                <%= image_tag(image["url"])%>
              <%else%>
                <%=image_tag("no_image.jpg")%>
              <%end%>
          <%end%>


        </div>
        <div class="movie_list">
          <h3>MOVIE LIST</h3>
          <%= link_to "編集する","/users/#{current_user.id}/movie_index"%>
        </div>
        <div class="book_list">
          <h3>BOOK LIST</h3>
          <%= link_to "編集する","/users/#{current_user.id}/book_index"%>
        </div>
      </div>
    </div>
    <div class="col-md-2">

    </div>
  </div>
</div>
